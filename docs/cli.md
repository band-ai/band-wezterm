<!-- Generated by docs/generate_cli_docs.py; do not edit. -->

# band

```console
band COMMAND
```

Manage Band rooms and detached agents in WezTerm.

## Table of Contents

- [`completion`](#band-completion)
- [`help`](#band-help)
- [`setup`](#band-setup)
- [`status`](#band-status)
- [`logs`](#band-logs)
- [`room`](#band-room)
    - [`open`](#band-room-open)
    - [`list`](#band-room-list)
    - [`create`](#band-room-create)
    - [`delete`](#band-room-delete)
- [`agent`](#band-agent)
    - [`list`](#band-agent-list)
    - [`create`](#band-agent-create)
    - [`configure`](#band-agent-configure)
    - [`start`](#band-agent-start)
    - [`stop`](#band-agent-stop)
    - [`status`](#band-agent-status)
    - [`delete`](#band-agent-delete)

**Commands**:

* [`agent`](#band-agent): Manage detached Band agents.
* [`completion`](#band-completion): Install shell completion for Band commands.
* [`help`](#band-help)
* [`logs`](#band-logs): Show recent rotating local diagnostics for incident investigation.
* [`room`](#band-room): Manage Band rooms.
* [`setup`](#band-setup): Install or update the Band WezTerm plugin in the active WezTerm config (WEZTERM_CONFIG_FILE, ~/.wezterm.lua, or XDG wezterm.lua).
* [`status`](#band-status): Show Rooms and Agents, or one requested resource table.

## band completion

```console
band completion [OPTIONS]
```

Install shell completion for Band commands.

**Parameters**:

* `--shell`: Shell type for completion. If not specified, attempts to auto-detect current shell. *[choices: zsh, bash, fish]*
* `--output, -o`: Output path for the completion script. If not specified, uses shell-specific default.

## band help

```console
band help
```

## band setup

```console
band setup
```

Install or update the Band WezTerm plugin in the active WezTerm config (WEZTERM_CONFIG_FILE, ~/.wezterm.lua, or XDG wezterm.lua).

## band status

```console
band status [OPTIONS]
```

Show Rooms and Agents, or one requested resource table.

**Parameters**:

* `--room, --no-room`: *[default: False]*
* `--agent, --no-agent`: *[default: False]*

## band logs

```console
band logs [OPTIONS]
```

Show recent rotating local diagnostics for incident investigation.

**Parameters**:

* `--tail`: *[default: 100]*

## band room

```console
band room COMMAND [OPTIONS]
```

Manage Band rooms.

**Commands**:

* [`create`](#band-room-create): Create a room with TITLE.
* [`delete`](#band-room-delete): Delete one room by exact title or unique ID prefix.
* [`list`](#band-room-list): List rooms by name prefix in a bounded page.
* [`open`](#band-room-open): Open one room by exact title or unique ID prefix.

**Parameters**:

* `--name`:
* `--limit`: *[default: 20]*
* `--offset`: *[default: 0]*

### band room open

```console
band room open REFERENCE
```

Open one room by exact title or unique ID prefix.

**Parameters**:

* `REFERENCE, --reference`: **[required]**

### band room list

```console
band room list [OPTIONS]
```

List rooms by name prefix in a bounded page.

**Parameters**:

* `--name`:
* `--limit`: *[default: 20]*
* `--offset`: *[default: 0]*

### band room create

```console
band room create TITLE
```

Create a room with TITLE.

**Parameters**:

* `TITLE, --title`: **[required]**

### band room delete

```console
band room delete REFERENCE
```

Delete one room by exact title or unique ID prefix.

**Parameters**:

* `REFERENCE, --reference`: **[required]**

## band agent

```console
band agent COMMAND [OPTIONS] [ARGS]
```

Manage detached Band agents.

**Commands**:

* [`configure`](#band-agent-configure): Open the reconfiguration surface for one existing agent.
* [`create`](#band-agent-create): Open the agent registration surface.
* [`delete`](#band-agent-delete): Stop and delete one managed agent.
* [`list`](#band-agent-list): List agents by name prefix in a bounded page.
* [`start`](#band-agent-start): Start one detached managed agent.
* [`status`](#band-agent-status): Show one managed agent's runtime state.
* [`stop`](#band-agent-stop): Gracefully stop one agent, or every agent with --all.

**Parameters**:

* `REFERENCE, --reference`: Exact agent name or a unique agent ID prefix.
* `--name`:
* `--limit`: *[default: 20]*
* `--offset`: *[default: 0]*
* `--verbose, --no-verbose, -v`: *[default: False]*

### band agent list

```console
band agent list [OPTIONS]
```

List agents by name prefix in a bounded page.

**Parameters**:

* `--name`:
* `--limit`: *[default: 20]*
* `--offset`: *[default: 0]*
* `--verbose, --no-verbose, -v`: *[default: False]*

### band agent create

```console
band agent create
```

Open the agent registration surface.

### band agent configure

```console
band agent configure REFERENCE
```

Open the reconfiguration surface for one existing agent.

**Parameters**:

* `REFERENCE, --reference`: **[required]**

### band agent start

```console
band agent start REFERENCE
```

Start one detached managed agent.

**Parameters**:

* `REFERENCE, --reference`: **[required]**

### band agent stop

```console
band agent stop [OPTIONS] [ARGS]
```

Gracefully stop one agent, or every agent with --all.

**Parameters**:

* `REFERENCE, --reference`:
* `--all, --no-all`: *[default: False]*

### band agent status

```console
band agent status REFERENCE
```

Show one managed agent's runtime state.

**Parameters**:

* `REFERENCE, --reference`: **[required]**

### band agent delete

```console
band agent delete REFERENCE
```

Stop and delete one managed agent.

**Parameters**:

* `REFERENCE, --reference`: **[required]**
